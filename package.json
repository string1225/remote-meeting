{
  "name": "remote-meeting",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Persistent dual-camera host with authenticated remote access and per-viewer WebRTC crops",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --watch --env-file-if-exists=.env server/index.js",
    "test": "node --test test/*.test.js",
    "test:e2e": "node test/browser.mjs",
    "test:accounts": "node test/accounts-browser.mjs",
    "check": "node test/check.mjs",
    "host": "node host/index.js"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
